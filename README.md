# Visual-Product-Matcher
An AI-powered web application that helps users find visually similar products by uploading an image or providing an image URL. The system analyzes the image using the Gemini API and returns the most relevant products from a curated database.
